# PAAW environment configuration
# Copy to .env and fill in values

# ── Bridge auth (tool proxy) ──
BRIDGE_TOKEN=

# ── Backup schedule ──
BACKUP_INTERVAL_MS=1800000   # 30 minutes

# ── Docker ──
COMPOSE_PROJECT_NAME=paaw
