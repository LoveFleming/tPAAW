# PAAW Production Instance — 與 dev (4097/4098/5173) 分離的 port
# 用法: npm run start:prod（= PAAW_ENV=prod + node scripts/prod-server.mjs）
# PAAW_ENV=prod 時 shared.mjs 優先載入本檔；UI dist 不烙 port（same-origin），任何 port 都正確
PAAW_PORT=4399
PAAW_WS_PORT=4400
BRIDGE_PORT=4402
