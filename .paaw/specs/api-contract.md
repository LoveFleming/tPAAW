```markdown
# API Contract

> Auto-generated by Code Understanding. Update by re-running.

## Conventions
- All responses: `{ "data": ... }` on success, `{ "error": { "code", "type", "message" } }` on failure
- Auth: Bearer token via `Authorization` header (unless noted)

---

## Endpoints

No API endpoints were detected in the project scan (`APIs: []`).  
This may be because the route/controller source files (e.g., `packages/server/src/routes/*.mjs`) are not visible in the file tree or are located outside the scanned directories.

To generate a complete contract, please ensure the following files are present and accessible:

- `packages/server/src/routes/handover.mjs`
- `packages/server/src/routes/code-health.mjs`
- `packages/server/src/routes/chat.mjs`

Then re-run the **Code Understanding** scan.

---

*If you have the source code for these routes, you can manually populate this document using the template below for each endpoint.*

### Endpoint Template (for reference)

```markdown
## POST /api/resource

**Purpose:** One sentence why this endpoint exists

### Request
- Content-Type: application/json
- Headers: Authorization (required)
- Body:
  | Field | Type | Required | Constraints | Description |
  |-------|------|----------|-------------|-------------|
  | name | string | yes | 1-100 chars | Resource name |

### Response
- **200 OK**
  ```json
  { "data": { "id": "string", "name": "string" } }
  ```
- **400 Bad Request** — Validation failed (→ Error Code 40001)
- **409 Conflict** — Resource exists (→ Error Code 40901)

### Example
```bash
curl -X POST /api/resource -H "Content-Type: application/json" -d '{"name":"test"}'
```

### Notes
- Any side effects, webhooks, or special behavior
```
```