# Error Code Documentation — tPAAW

## Analysis Result

Based on the project scan, **no error codes were found** in the source code. The file tree only contains temporary payload/stream files and git history. No actual source code files (`.ts`, `.tsx`, `.js`, `.jsx`) were provided for analysis.

## Gap Analysis

| Gap | Status | Impact |
|-----|--------|--------|
| Error code definitions | ❌ Missing | Cannot generate error map |
| Error handling middleware | ❌ Missing | Cannot document flow |
| HTTP status mappings | ❌ Missing | Cannot create runbooks |
| Source code access | ❌ Missing | Cannot analyze patterns |

## Required Actions

### 1. Provide Source Code
To generate accurate error documentation, I need access to:
- `src/` or `app/` directory containing TypeScript/JavaScript files
- Error handling middleware files
- Custom exception/error classes
- API route handlers

### 2. Expected File Locations (based on project structure)
```
.paaw/
├── specs/
│   └── error-codes.md          # Error mapping table
└── runbook/
    ├── 40001.md                # Validation error
    ├── 40901.md                # Conflict error
    └── 50001.md                # Internal error
```

## Template: Error Code Map (when source is available)

```markdown
# Error Code Mapping

> Auto-generated by Code Understanding.

## HTTP Error Codes

| Code | Type | Severity | HTTP | Trigger | Recovery | Runbook |
|------|------|----------|------|---------|----------|---------|
| 40001 | VALIDATION_ERROR | low | 400 | Invalid input field | Fix input, retry | [→](../runbook/40001.md) |
| 40901 | CONFLICT | medium | 409 | Duplicate resource | Check existing | [→](../runbook/40901.md) |
| 50001 | INTERNAL | high | 500 | Unhandled exception | Check logs | [→](../runbook/50001.md) |

## Error Handling Flow

1. Business logic throws `AppException(code, type, message)`
2. Middleware catches → maps to HTTP status
3. Response: `{ "error": { "code", "type", "message" } }`
4. Client checks error code → looks up runbook
```

## Template: Runbook (when source is available)

```markdown
# Runbook: {CODE} — {TYPE}

## What Happened
Plain language description of what this error means.

## Symptoms
- What the user sees
- What the API returns
- What shows in logs

## Root Causes (ranked by frequency)
1. Most common cause
2. Less common cause

## Resolution Steps

### Quick Fix (try first)
Step-by-step actions to resolve.

### Deep Debug (if quick fix fails)
1. Check logs: `grep "CODE" logs/`
2. Check database state
3. Check upstream dependencies

## Prevention
How to avoid this error in the future.

## Related
- API endpoint(s) that can trigger this
- Related error codes
```

## Next Steps

1. **Provide source code** — Share the actual codebase files (especially error handling, middleware, and API routes)
2. **Define error codes** — If not yet implemented, create a standard error code system
3. **Implement middleware** — Add error handling middleware that catches and maps errors
4. **Generate documentation** — Once source is available, I can generate the complete error map and runbooks

## Recommendation

Based on the git log showing active development of an AI agent management platform, I recommend:

1. **Create error code standards** before implementing error handling
2. **Use a consistent pattern** like `AppException` with code, type, message
3. **Document as you code** — Add error codes to `.paaw/specs/error-codes.md` during development
4. **Generate runbooks** for each error code as they're implemented

Would you like me to help design the error code system for your project, or can you provide the source code for analysis?