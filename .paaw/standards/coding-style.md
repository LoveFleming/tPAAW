```markdown
# Coding Standards

> Auto-generated by Code Understanding. Inferred from actual codebase patterns.

## Language & Runtime
- Language: TypeScript (Node.js 18+ inferred)
- Runtime: Node.js / browser (React SPA + Express API)
- Package manager: npm (or pnpm if monorepo workspace setup; default to npm)

## Project Structure
```
packages/
  ui/
    src/
      components/        # React components (.tsx)
  server/
    src/
      routes/            # Express route handlers (.mjs)
      lib/               # Business logic, tools, config (.mjs)
      shared/            # Shared types and constants (.mjs/.ts)
.paaw/                   # Project configuration, knowledge base
temp/                    # Temporary payload/logs (should not be committed)
```

## Naming Conventions
| Element | Convention | Example |
|---------|-----------|---------|
| Files (server routes/lib) | kebab-case | `code-health.mjs` |
| Files (React components) | PascalCase | `EMDashboard.tsx` |
| Functions | camelCase | `loadUserProfile()` |
| Constants | UPPER_SNAKE | `MAX_RETRIES` |
| Types/Interfaces | PascalCase | `TaskRecord` |
| API routes | kebab-case | `/api/coding-project` |

## Code Style
- Indentation: 2 spaces (inferred from TypeScript/React defaults)
- Quotes: single quotes (common in ES modules)
- Semicolons: required (inferred from typical TypeScript config)
- Trailing commas: yes (modern JS style)

> Note: These are inferred from typical project conventions. If lint/format config exists (e.g., Prettier), it takes precedence.

## Error Handling
- Server routes use try-catch blocks; errors are returned as JSON with status codes.
- No explicit error code enum detected yet — all new errors must define an error code in `shared/error-codes.ts`.
- Every error code → must have runbook in `.paaw/runbook/`.

## Cross-Platform Rules (if applicable)
- File paths must use `path.join()` or `path.resolve()` (not string concatenation with `/`).
- Line endings: LF (Unix) — ensure `.gitattributes` enforces.

## Git Conventions
- Commit format: `type: description` (types observed: `fix`, `feat`, `cleanup`, `refactor`)
- Branch naming: `feature/xxx`, `fix/xxx`, `chore/xxx`
- Rule: always commit and push after changes (no lingering local modifications).

## i18n Rules (if applicable)
- Not used in this project (single user, English-only UI).

## .paaw/ Knowledge Base Rules
- `PROJECT.md` — project overview (update when scope changes)
- `ARCHITECTURE.md` — module structure (update when architecture changes)
- `specs/api-contract.md` — API documentation (update when endpoints change)
- `specs/error-codes.md` — error mapping (update when new errors added)
- `standards/` — this file + project-specific standards (e.g., component styles, state management patterns)

```