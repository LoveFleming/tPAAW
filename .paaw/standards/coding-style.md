```markdown
# Coding Standards

> Auto-generated by Code Understanding. Inferred from actual codebase patterns (git log, architecture docs, decision records).  
> *Note: Source code files were not provided in the scan. Conventions below are based on project metadata and common React/Next.js/TypeScript patterns.*

## Language & Runtime
- **Language:** TypeScript (v5.x inferred from modern React/Next.js stack)
- **Runtime:** Node.js (LTS version, likely 18+)
- **Package manager:** pnpm (inferred from typical t3 stack usage; verify `package.json` or `pnpm-lock.yaml`)

## Project Structure
```
.
├── .paaw/                  # Knowledge base (decisions, architecture, feature maps, standards)
│   ├── ARCHITECTURE.md
│   ├── DECISIONS.md
│   ├── PROJECT.md
│   ├── specs/
│   │   ├── api-contract.md  (expected)
│   │   └── error-codes.md   (expected)
│   └── standards/           (this file)
├── src/                     # Source code (inferred)
│   ├── app/                 # Next.js App Router (inferred)
│   ├── components/          # UI components
│   └── lib/                 # Utilities, tools, agent logic
├── temp/                    # Temporary payload/stream files (not in version control)
├── public/                  # Static assets
├── package.json
├── tsconfig.json
└── next.config.js
```

## Naming Conventions
| Element | Convention | Example |
|---------|-----------|---------|
| Files & directories | kebab-case | `feature-mapping-panel.tsx`, `paaw-tree.ts` |
| Functions & variables | camelCase | `loadFeatureMap()`, `activeIssueId` |
| Constants (true constants) | UPPER_SNAKE_CASE | `MAX_AGENTS`, `PAW_DIR` |
| Types & Interfaces | PascalCase | `FeatureMapEntry`, `IssueRecord` |
| React components | PascalCase | `IssueTrackerPanel`, `AgentMemory` |
| Props interfaces | `ComponentNameProps` | `IssueTrackerPanelProps` |
| API routes | kebab-case | `/api/feature-map`, `/api/issue-tracker` |
| Git branches | `feature/xxx`, `fix/xxx`, `chore/xxx` | `feature/night-shift-agent` |

## Code Style
- **Indentation:** 2 spaces (standard TypeScript/React)
- **Quotes:** single quotes (`'`) for strings; backticks for template literals
- **Semicolons:** required
- **Trailing commas:** required (ES2017+ convention)
- **JSX:** double quotes for JSX attributes (`className="my-class"`)
- **Import ordering:** absolute imports before relative; external before internal

## Error Handling
- All user-facing errors go through an `AppError` class or a custom error enum (inferred from architecture's `error-codes.md`).
- Errors are caught at the API boundary and returned as structured JSON responses with an `errorCode` field.
- Every error code **must** have a corresponding runbook entry in `.paaw/specs/error-codes.md`.
- Agent actions log errors to `temp/stream-<timestamp>.log` for debugging.

## Cross-Platform Rules (if applicable)
- Path handling uses `path.posix` (forward slashes) even on Windows; avoid `path.join` with backslashes.
- All file operations go through a centralized `fileSystem` utility to ensure consistent encoding (UTF-8) and error handling.

## Git Conventions
- **Commit format:** `type: description` (e.g., `feat: add Night Shift mode`, `fix: correct useI18n import`)
- **Types:** `feat`, `fix`, `chore`, `docs`, `refactor`, `test`, `style`
- **Branch naming:** `<type>/<short-description>` (e.g., `feature/agent-memory-panel`)
- **Rule:** always commit and push after changes (no local work-in-progress)

## i18n Rules
- All user-visible strings **must** use the `t()` function from `useI18n` hook.
- Key format: `category.subcategory.element` (e.g., `issueTracker.placeholder.empty`)
- Supported locales: `en` (primary), `zh` (secondary, inferred from commit messages)
- New strings must be added to locale files (likely in `public/locales/` or `src/i18n/`)

## .paaw/ Knowledge Base Rules
- `PROJECT.md` — update when project scope changes
- `ARCHITECTURE.md` — update when module structure or data flow changes
- `specs/api-contract.md` — update when endpoints change (request/response schemas)
- `specs/error-codes.md` — update when new error codes are added
- `standards/` — this file + any project-specific standards (update when new patterns emerge)
- New decisions must be recorded in `DECISIONS.md` using ADR format (context/decision/consequences/alternatives)

## Project-Specific Conventions
- **Agent-panel integration:** Every UI panel that displays agent information must show the agent's avatar and name (see commit `6639153`).
- **Feature Map auto-update:** The feature map is updated by agents on every code change (commit `f598e40`); manual refresh is also available.
- **Night Shift mode:** When enabled, an Engineering Manager agent leads a 6-agent team; all team members must be shown with avatars and status in the Agent Memory panel.
- **Issue tracker string:** Remember the function is called `t`, not `tt` (see commit `93e1d47`).
- **File-based knowledge store:** All project knowledge lives in `.paaw/` as markdown/JSON; never use an external database (ADR-001).
- **Structured agent tools:** Agents must use `.paaw/` API tools (e.g., `read_feature_map`) instead of raw file reads (ADR-002).

> **Note on detection gaps:** Actual source code was not available during generation. The above conventions are inferred from commits, architecture docs, and decision records. Once source files are present, run code understanding again to verify indentation, quote style, and other patterns.
```