{
  "id": "memory",
  "name": "記憶",
  "icon": "🧠",
  "description": "長期記憶管理，跨對話保留",
  "status": "published",
  "dataShape": "array",
  "schema": {
    "items": {
      "properties": {
        "key": { "type": "string", "required": true },
        "content": { "type": "string", "required": true }
      }
    }
  },
  "aiPrompt": "長期記憶。使用者說「記住」→ 存到記憶。內容精簡有結構。同時更新 MEMORY.md。"
}
