{
  "name": "我的待辦",
  "description": "簡單實用的待辦事項管理工具，資料儲存在伺服器端",
  "icon": "✅",
  "template": "todo",
  "skillId": "todo",
  "version": "2.0.0",
  "generatedAt": "2026-06-04T14:00:00.000Z",
  "publishedAt": "2026-06-04T14:00:00.000Z",
  "status": "published",

  "schema": {
    "type": "array",
    "items": {
      "type": "object",
      "properties": {
        "id":       { "type": "string", "description": "唯一識別碼", "readOnly": true },
        "text":     { "type": "string", "description": "待辦事項內容", "required": true },
        "done":     { "type": "boolean", "description": "是否完成", "default": false },
        "priority": { "type": "string", "enum": ["low", "medium", "high"], "description": "優先順序", "default": "medium" },
        "tags":     { "type": "array", "items": { "type": "string" }, "description": "標籤" },
        "createdAt":    { "type": "string", "format": "date-time", "description": "建立時間", "readOnly": true },
        "completedAt":  { "type": "string", "format": "date-time", "description": "完成時間" }
      },
      "required": ["text"]
    }
  },

  "api": {
    "endpoint": "/api/app-data/todo",
    "methods": ["GET", "POST", "PUT", "PATCH", "DELETE"],
    "description": "使用萬用 App Data API，appId = todo"
  },

  "aiContext": {
    "humanName": "我的待辦",
    "summary": "這是使用者的待辦事項清單，可以新增、完成、刪除待辦事項",
    "examples": {
      "query": "GET /api/app-data/todo → 取得所有待辦",
      "add": "POST /api/app-data/todo, body: {\"text\":\"買牛奶\"} → 新增待辦",
      "toggle": "PATCH /api/app-data/todo/{id}, body: {\"done\":true} → 標記完成",
      "delete": "DELETE /api/app-data/todo/{id} → 刪除待辦"
    }
  }
}
