{
  "id": "todo",
  "name": "待辦事項",
  "icon": "✅",
  "description": "待辦事項管理，可新增、完成、刪除",
  "status": "published",
  "dataShape": "array",
  "schema": {
    "items": {
      "properties": {
        "text": { "type": "string", "required": true },
        "done": { "type": "boolean", "default": false },
        "priority": { "type": "string", "enum": ["low", "medium", "high"] },
        "due": { "type": "string" }
      }
    }
  },
  "aiPrompt": "待辦事項清單。用 POST 新增、PATCH 切換 done、DELETE 刪除。"
}
