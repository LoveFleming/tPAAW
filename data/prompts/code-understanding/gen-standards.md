# Code Understanding — Generate Coding Standards

You are a tech lead. Document the project's actual coding conventions so any developer (or AI agent) writes code that fits.

## What You Receive
- Project scan results
- Source code samples (multiple files)
- Existing lint/format config (eslint, prettier, tsconfig, etc.)
- Existing .paaw/standards/ (if any)

## What to Produce

Save as `.paaw/standards/coding-style.md`:

```markdown
# Coding Standards

> Auto-generated by Code Understanding. Inferred from actual codebase patterns.

## Language & Runtime
- Language: {language} ({version if detectable})
- Runtime: {runtime}
- Package manager: {npm/pnpm/yarn}

## Project Structure
```
{actual directory tree, 2 levels deep}
```

## Naming Conventions
| Element | Convention | Example |
|---------|-----------|---------|
| Files | kebab-case | `task-persistence.mjs` |
| Functions | camelCase | `loadUserProfile()` |
| Constants | UPPER_SNAKE | `MAX_RETRIES` |
| Types/Interfaces | PascalCase | `TaskRecord` |
| API routes | kebab-case | `/api/coding-project` |

## Code Style
- Indentation: {detected} spaces
- Quotes: {detected}
- Semicolons: {detected}
- Trailing commas: {detected}

## Error Handling
- {How errors are thrown and caught in this project}
- {Error class/enum used}
- Every error code → must have runbook in `.paaw/runbook/`

## Cross-Platform Rules (if applicable)
- {Any path handling rules, OS-specific code patterns}

## Git Conventions
- Commit format: `{type}: {description}`
- Branch naming: `feature/xxx`, `fix/xxx`
- Rule: always commit + push after changes

## i18n Rules (if applicable)
- All user-visible strings must use `t()` function
- Key format: `category.subcategory`
- Supported locales: {list}

## .paaw/ Knowledge Base Rules
- `PROJECT.md` — project overview (update when scope changes)
- `ARCHITECTURE.md` — module structure (update when architecture changes)
- `specs/api-contract.md` — API documentation (update when endpoints change)
- `specs/error-codes.md` — error mapping (update when new errors added)
- `standards/` — this file + project-specific standards
```

## Rules
- **Infer from ACTUAL code** — read real files, detect real patterns
- If the project is inconsistent, state the preferred style and mark it
- Include project-specific conventions (not generic best practices)
- Cross-reference `DECISIONS.md` for things that have an ADR
- Keep it as a quick-reference, not a tutorial
